import { dashBoardCards } from "../pages/dashboard/DashboardPage";
export type trackerStatus = "Review" | "HR Interview" | "User Interview" | "Offer" | "Accept" | "Rejected";
const selectStatusColor = (status : trackerStatus) => {
  
}
